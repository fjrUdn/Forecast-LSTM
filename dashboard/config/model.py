# Get the base models path, for now on ./models
BASE_PATH = "./dashboard/models"

# Name of the model file
MODEL_FILE_NAME = f"{BASE_PATH}/bestModel_lstm.h5"

# Name of the scale file
SCALE_FILE_NAME = f"{BASE_PATH}/scaler.pkl"
